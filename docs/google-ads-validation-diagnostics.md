# Google Ads validation diagnostics

Validate-only conversion checks surface only the first safe authentication or Google API error message. OIDC tokens and customer match hashes are never logged or persisted by this diagnostic path.
